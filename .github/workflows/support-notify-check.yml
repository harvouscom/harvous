name: Support Ticket Notify Check

on:
  schedule:
    # Run hourly
    - cron: '0 * * * *'
  workflow_dispatch: # Allow manual triggering from GitHub Actions UI

jobs:
  support-notify-check:
    runs-on: ubuntu-latest
    steps:
      - name: Call Support Notify-Check API
        env:
          SITE_URL: ${{ secrets.AUTO_ARCHIVE_SITE_URL || 'https://app.harvous.com' }}
          SECRET_TOKEN: ${{ secrets.SUPPORT_NOTIFY_SECRET_TOKEN }}
        run: |
          echo "Calling notify-check endpoint: $SITE_URL/api/admin/support/notify-check"
          if [ -z "$SECRET_TOKEN" ]; then
            response=$(curl -s -w "\n%{http_code}" -X POST $SITE_URL/api/admin/support/notify-check)
            echo "$response"
          else
            response=$(curl -s -w "\n%{http_code}" -X POST $SITE_URL/api/admin/support/notify-check \
              -H "Authorization: Bearer $SECRET_TOKEN")
            echo "$response"
          fi
