[build]
  command = "npm ci && npm run db:push && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.18.0"
  NPM_FLAGS = "--prefer-offline --no-audit"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security Headers - TEMPORARILY DISABLED FOR DEVELOPMENT
# TODO: Re-enable for production with conditional logic based on CONTEXT env var
# The Netlify Vite plugin applies these headers during `npm run dev` which blocks Clerk
# We need to find a way to only apply CSP in production, not development

# [[headers]]
#   for = "/*"
#   [headers.values]
#     # Content Security Policy - PRODUCTION
#     # Allows all necessary resources for Clerk, PostHog, fonts, and app functionality
#     Content-Security-Policy = "default-src 'self' 'unsafe-inline' 'unsafe-eval' https: data: blob: wss:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https: https://*.clerk.com https://*.posthog.com https://*.us.posthog.com; worker-src 'self' blob:; child-src 'self' blob:; style-src 'self' 'unsafe-inline' https: https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https: blob:; connect-src 'self' https: wss: https://*.clerk.com https://*.clerk.accounts.dev https://*.posthog.com https://*.us.posthog.com https://api.bible.org; frame-src 'self' https://*.clerk.com; object-src 'none';"
#     # Prevent clickjacking
#     X-Frame-Options = "DENY"
#     # Prevent MIME type sniffing
#     X-Content-Type-Options = "nosniff"
#     # Referrer policy
#     Referrer-Policy = "strict-origin-when-cross-origin"
#     # Permissions policy
#     Permissions-Policy = "geolocation=(), microphone=(), camera=()"
#     # XSS Protection (legacy, but still useful)
#     X-XSS-Protection = "1; mode=block"
#     # Strict Transport Security (HSTS) - only for HTTPS, won't affect localhost
#     Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# Note: For scheduled functions, configure in Netlify dashboard:
# Site Settings > Functions > Scheduled Functions
# Or use an external cron service to call /api/inbox/auto-archive daily
