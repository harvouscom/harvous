/**
 * Icon box sizes for `/prototype/` chrome — Font Awesome Solid via `Icon` (fill-based, no stroke).
 *
 * Toolbar and space-trigger buttons use a 15pt box aligned with `Icon`; list-mode shares the same
 * for the Notes/Folders menu trigger.
 */
export const PROTO_TOOLBAR_ICON_SIZE = 15;

/** List view mode trigger (`ListViewMenu` notes / folders icon) — kept equal to toolbar for parity. */
export const PROTO_LIST_VIEW_ICON_SIZE = 15;
