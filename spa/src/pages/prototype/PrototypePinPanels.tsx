/**
 * Prototype shell — account PIN unlock/lock flows (production uses DesktopPanelManager).
 * Temporarily disabled while note lock is off in the prototype.
 */
export default function PrototypePinPanels() {
  return null;
}
