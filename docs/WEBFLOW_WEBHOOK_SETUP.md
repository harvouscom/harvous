# Webflow Webhook Setup for Inbox Sync (RETIRED)

> **Retired.** Inbox content is no longer synced from Webflow CMS. Use the Featured Item system instead — see [FEATURED_ITEM_SYSTEM.md](./FEATURED_ITEM_SYSTEM.md).
>
> The `/api/webflow/webhook` and `/api/webflow/sync-inbox` endpoints have been removed. Disable any remaining Webflow webhooks pointing at Harvous and remove unused `WEBFLOW_*` env vars when convenient.
