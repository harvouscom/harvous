---
// Basic functioning version of ThreadsDrawer
interface Props {
  noteId: number;
  userId: string;
}

const { noteId, userId } = Astro.props;

// Stub data until we fix the real component
const noteThreadIds = [];
---

<div
  x-data="{ 
    isOpen: false,
    activeTab: 'available',
    drawerId: 'threads'
  }"
  x-cloak
  class="fixed inset-0 z-50 flex items-end justify-center"
  @show-drawer.window="isOpen = $event.detail && $event.detail.drawer === drawerId"
  @hide-drawer.window="isOpen = false"
>
  <!-- Just a stub component for now -->
</div>

<style>
  /* Basic styles */
  button {
    transition: all 0.2s ease;
  }
</style> 