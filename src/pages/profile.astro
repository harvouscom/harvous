---
import Layout from "@/layouts/Layout.astro";
import CardStack from "@/components/CardStack.astro";
import NavigationColumnReact from "@/components/NavigationColumnReact.astro";
import MobileNavigation from "@/components/MobileNavigation.astro";
import MobileAdditional from "@/components/MobileAdditional.astro";
import { getAllThreadsWithCounts, getSpacesWithCounts, getInboxCount } from "@/utils/dashboard-data";
import { getSeasonalXP, getLifetimeXP } from "@/utils/xp-system";
import { getSeasonDisplayName } from "@/utils/season-helpers";
import { getThreadGradientCSS } from "@/utils/colors";
import { getCachedUserData, type CachedUserData } from '@/utils/user-cache';
import { db, UserMetadata, eq } from 'astro:db';
import ProfilePageReact from "@/components/ProfilePageReact.astro";
import ProfileContent from "@/components/profile/ProfileContent.astro";
import ProfileCardStackHeader from "@/components/react/ProfileCardStackHeader.tsx";
import { markdownToHtml } from '@/utils/markdown-to-html';
import founderLetterMarkdown from '@/data/about/founder-letter.md?raw';

// Static build - all user data and navigation data loaded client-side
// Components handle their own auth via API calls with JWT tokens
const userData: CachedUserData = {
  firstName: '',
  lastName: '',
  email: '',
  initials: 'U',
  displayName: 'User',
  userColor: 'paper',
};

const { firstName, lastName, email, initials, displayName, userColor } = userData;

// Format join date
const currentDate = new Date();
const currentMonth = currentDate.toLocaleDateString('en-US', { month: 'short' });
const currentYear = currentDate.getFullYear();
const joinDate = `${currentMonth} ${currentYear}`;

// Static build - navigation data loaded client-side
const spaces: any[] = [];
const threads: any[] = [];
const inboxCount = 0;
const seasonalXP = 0;
const lifetimeXP = 0;
const seasonName = getSeasonDisplayName();
const churchData = {
  churchName: null as string | null,
  churchCity: null as string | null,
  churchState: null as string | null
};

const contentType = "profile";
const contentId = "profile";

// Extract publishable key in frontmatter (where import.meta works)
const publishableKey = import.meta.env.PUBLIC_CLERK_PUBLISHABLE_KEY || null;

// Load and parse the founder letter markdown
// Use ?raw import to get the raw markdown content at build time
const founderLetterHtml = markdownToHtml(founderLetterMarkdown);
---

<Layout
  title="Profile"
  contentType={contentType}
  contentId={contentId}
  userData={{
    email: userData.email,
    name: `${userData.firstName} ${userData.lastName}`.trim(),
    displayName: userData.displayName,
    userColor: userData.userColor,
  }}
  founderLetterHtml={founderLetterHtml}
>
  <!-- Navigation Column -->
  <NavigationColumnReact 
    slot="navigation"
    inboxCount={inboxCount}
    spaces={spaces}
    activeThread={null}
    currentSpace={null}
    isNote={false}
    currentId={undefined}
    showProfile={true}
    initials={initials}
    userColor={userColor}
  />

  <!-- Main Column -->
  <div slot="main" class="h-full">
    <div class="flex flex-col h-full">
      <div class="flex-1 min-h-0">
        <CardStack 
          centerTitle={true}
          class="h-full"
          id="profile-cardstack"
        >
          <ProfileCardStackHeader 
            slot="header"
            initialColor={userColor}
            initialDisplayName={displayName}
            client:load
          />
          <ProfileContent joinDate={joinDate} seasonalXP={seasonalXP} seasonName={seasonName} />
        </CardStack>
      </div>
      
      <!-- Logout Button at Bottom -->
      <div class="mt-4 mb-safe">
        <a
          id="logout-button"
          href="#"
          data-outer-shadow
          class="btn-cta btn--secondary flex-1 group no-underline"
          onclick="event.preventDefault(); if (window.Clerk && window.Clerk.signOut) { window.Clerk.signOut({ redirectUrl: '/sign-in' }); } else { window.location.href = '/sign-in'; } return false;"
        >
          <span class="btn-cta__content">
            Logout
          </span>
          <div class="btn-cta__shadow" />
        </a>
      </div>
    </div>
  </div>

  <ProfilePageReact
    slot="additional"
    displayName={displayName}
    userColor={userColor}
    joinDate={joinDate}
    seasonalXP={seasonalXP}
    lifetimeXP={lifetimeXP}
    seasonName={seasonName}
    firstName={firstName}
    lastName={lastName}
    spaces={spaces}
    churchData={churchData}
    publishableKey={publishableKey}
    founderLetterHtml={founderLetterHtml}
  />

  <!-- Mobile Navigation -->
  <div slot="mobile-navigation">
    <MobileNavigation 
      spaces={spaces}
      threads={threads}
      inboxCount={inboxCount}
      currentSpace={null}
      currentThread={null}
      initials={initials}
      userColor={userColor}
    />
  </div>

  <!-- Mobile Additional -->
  <div slot="mobile-additional">
    <MobileAdditional contentType="profile" contentId={contentId} currentThread={null} currentSpace={null} />
  </div>
</Layout>
