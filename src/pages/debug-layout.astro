---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Debug Layout">
  <div class="max-w-4xl mx-auto p-6">
    <h1 class="text-3xl font-bold mb-6">Debug Layout</h1>
    <p class="text-gray-600 mb-8">
      This page should show the add button in the additional column.
    </p>
    
    <div class="bg-white rounded-2xl shadow-lg p-6">
      <h2 class="text-xl font-semibold mb-4">Debug Information</h2>
      <p class="text-gray-700 mb-2">
        Check the browser console for debug logs about panel state.
      </p>
      <p class="text-gray-700 mb-2">
        The add button should be visible in the right column.
      </p>
      <p class="text-gray-700">
        If you don't see it, check if localStorage has showNewNotePanel set to true.
      </p>
    </div>
    
    <div class="mt-6">
      <button 
        onclick="localStorage.removeItem('showNewNotePanel'); localStorage.removeItem('showNewThreadPanel'); location.reload();"
        class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600"
      >
        Clear localStorage and Reload
      </button>
    </div>
  </div>
</Layout>
