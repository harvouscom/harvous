---
import MobileAdditionalReact from "./react/MobileAdditional";

export interface Props {
  contentType?: "thread" | "note" | "space" | "dashboard" | "profile";
  contentId?: string;
  currentThread?: any;
  currentSpace?: any;
  noteType?: string;
  contentEncrypted?: boolean;
  noteSimpleId?: number | null;
}

const { contentType = "dashboard", contentId, currentThread, currentSpace, noteType, contentEncrypted, noteSimpleId } = Astro.props;
---

<MobileAdditionalReact
  contentType={contentType}
  contentId={contentId}
  currentThread={currentThread}
  currentSpace={currentSpace}
  currentThreadId={currentThread?.id}
  noteType={noteType}
  contentEncrypted={contentEncrypted}
  noteSimpleId={noteSimpleId}
  client:load
/>
