# new.harvous.com root → prototype shell (absolute URLs; force beats index.html shadowing)
https://new.harvous.com/  https://new.harvous.com/prototype/  302!
http://new.harvous.com/   https://new.harvous.com/prototype/  302!

# API routes → Hono Netlify Function
/api/*  /.netlify/functions/api/:splat  200

# SPA catch-all — everything else serves index.html (React Router handles routing)
# Static assets (JS, CSS, images, fonts) are served directly by Netlify before this rule applies.
/*  /index.html  200
