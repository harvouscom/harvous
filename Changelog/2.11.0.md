# Version 2.11.0

**Release Date**: July 31, 2026

## Features

- Per-space co-editing: `SpaceNotes.coEditEnabled` is the source of truth; `Notes.coEditEnabled` is an OR-mirror for native / live bodies
- Inspector Spaces section: each shared association has its own "Let others edit" toggle (Info Editing row removed)
- `PATCH /api/notes/:id/co-edit` requires `spaceId`

## Notes

- Collaborator may write if they belong to any live shared association with co-edit on
- Locking clears all association grants + the note mirror
- SQL: `server/scripts/sql/add-space-notes-co-edit.sql` (applied + backfill from prior note-level flag)
