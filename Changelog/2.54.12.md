# Version 2.54.12

**Release Date**: August 13, 2026

## Internal

- Add a test that fails the build when a `.proto-marquee` label renders zero pixels wide
- Give the marquee CI job a placeholder Clerk key so the SPA boots on a runner
- Allow the release-notes sync workflow to be run by hand
- Stop tracking Playwright run artifacts

<!--
  Internal, not Fixes. Nothing here is visible to a reader — it is a regression test, the CI job
  that runs it, a workflow trigger, and a .gitignore entry. As Fixes it published four rows to
  harvous.com's release-notes page, including "Stop tracking Playwright run artifacts".

  This version exists because the commit was typed `fix(ci):`, which bump-version.js treats as a
  patch. Second time in one day. Infrastructure work is `chore:` — it does not bump and never
  reaches this folder.
-->
