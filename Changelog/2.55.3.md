# Version 2.55.3

**Release Date**: August 13, 2026

## Internal

- Name the save rejection for a body with no note id as `NOTE_ID_REQUIRED`

## Documentation

- Write the day's note for 2.55, and fix the titles it publishes

<!--
  Internal, not Fixes. Nothing here is visible to a reader: the request already failed, with the
  same status and the same message, and `classifySaveFailure` branches on status alone — the new
  `code` only helps whoever is reading the response while diagnosing. As a Fix it would have
  published "A save rejected for a missing id says which failure it was" to the release-notes
  page, which describes a JSON field.

  The bullet was also the commit subject verbatim: bump-version.js lifts it, and every non-private
  bullet becomes a public title. See .claude/agents/marketing.context.md.
-->
