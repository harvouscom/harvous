# Version 2.54.11

**Release Date**: August 13, 2026

## Internal

- Sync release notes to the marketing site with a deploy key instead of an expiring token

<!--
  Filed under Internal rather than Fixes on purpose. Only Features and Fixes are exported to
  harvous.com's public release-notes feed (scripts/export-changelog-csv.js), and this release
  changes nothing a Harvous user can see — as a Fix it published the line "We've fixed an issue:
  Sync release notes with a deploy key instead of an expiring token", which is a sentence about
  our CI.

  This version exists at all because the commit was typed `fix(ci):`, and bump-version.js bumps
  on any `fix:`. Infrastructure work should be `chore:`, which does not bump and never reaches
  this folder.
-->
