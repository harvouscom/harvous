# Version 2.18.0

**Release Date**: August 4, 2026

## Features

- Church org: congregant receive loop — browse and follow ministry channels, "From your church" study feed on Home
- Church org: billing with a concierge pilot window; sponsorship gates writes, never reads
- Church org: self-serve staff management via Clerk org invites, with webhook-driven reconciliation
- Church org: role-gated capabilities derived server-side, plus org-provisioned note templates

## Improvements

- Sidebar navigation: one parent context (`ProtoLocation`) replaces the mutually-exclusive `activeSpaceId` / `activeChurchOrgId` pair
- Sidebar navigation: flat space switcher — parents and their spaces in a single list, every row one click
- Sidebar navigation: menu rows share one left edge; reorder handle moved out of flow
- Ministry channel header: church name clamped to one line, revealed on hover
