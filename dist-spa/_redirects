# No catch-all needed — the Astro SSR function (path:'/*', preferStatic:true)
# handles all routes. Static assets are served directly; everything else
# goes through the function which handles both /api/* and SPA routes.
